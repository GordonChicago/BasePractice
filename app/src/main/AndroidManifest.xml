<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.basepractice">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPropertyTest" />
        <activity android:name=".MeasuedViewActivity" />
        <activity android:name=".MScrollViewActivity" />
        <activity android:name=".EventDispatchActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".ViewXYActivity" />
        <activity android:name=".MScrollViewImpl" />
        <activity android:name=".ValueAnimator" />
        <activity android:name=".VolleyTestActivity" />
        <activity android:name=".AndroidDrawTest"/>
        <activity android:name=".ViewFocusTest"/>
    </application>

</manifest>